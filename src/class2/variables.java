package class2;

public class variables {
    public static void main(String[] args) {
        /*
        below line is creating a box in the computers memories.
        we are calling its box1 if we need the information that we are storing inside this box
        we are simply say to the computer give us a content of box1
        int= what type of data we want to store
        box1 is a name of the box
        = sing will take whatever we write after it and will store it inside the box1
        10= what we are storing inside the box1
         */
        int box1=10;
        System.out.println(box1);//bring the content of box1 and print them on the console


    }
}
